__pycache__/
*.pyc
~$*
.DS_Store

# R1.1 raw HRRR alpha dumps (regenerable via fetch_alpha_raw.py, ~29MB each)
docs/figures/wind_v3/alpha_raw_*.npz

# Bulky Grid SCED data / generated results — commit source only
Grid/grid_data/
Grid/binding_constraints_7y.csv

# Personal documents — never track
Admin/

# Abandoned Stage A/B joint-load+wind experiment — kept locally as a record only,
# never tracked (see Claude_load.md for why it was moot). The one shared data
# aligner it used now lives at experiments/_shared/load_wind_io.py.
experiments/stage_a_joint_load_wind/
experiments/stage_b_conditional_loads/
